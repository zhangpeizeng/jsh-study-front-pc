import { Component, Prop, Vue } from "vue-property-decorator";
import AliyunUploadVideo from "@/utils/aliyunUploadVideo";

export default class tableHeaderIcon extends Vue {
  private created() {
    //将资源按本地上传 实景选择 视频分为三类
  }
}
